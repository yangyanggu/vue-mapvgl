import { withInstall } from '../../utils/with-install'
import HeatPointLayer from './HeatPointLayer.vue'

export const ElBmapvHeatPointLayer = withInstall(HeatPointLayer)
export default ElBmapvHeatPointLayer
